/**
 * Common Components
 * Reusable components that can be used across the application
 */

export { MarkdownPreview } from "./MarkdownPreview"

// Re-export SearchResults from search components for backward compatibility
export { SearchResults, type SearchResult } from "../search/SearchResults"
