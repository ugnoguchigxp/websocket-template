apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: wsfw
  labels:
    app: api
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: api
